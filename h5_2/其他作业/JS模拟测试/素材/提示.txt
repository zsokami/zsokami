第一题提示：
如果在添加行后，新添加的行没有绑定事件了，可以尝试以下方法：
例如：之前给表格中的行绑定了click事件的方式是：
	$('table tr').click(funcktion(){
 	        ......
	});
       如果希望新添加的行也有click事件，可试试以下方法：
              $(document).on('click','table tr',function(){
	      .......
	})